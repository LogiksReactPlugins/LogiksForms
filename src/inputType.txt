dataMethod,
dataSelector,
autosuggest,
dataSelectorFromTable,
selectAJAX,

tags,
radiolist,(radio button)
richtextarea,(textarea with formatter)
markup,
datetime(is it datetime-local ?),
year(yeaer inpuut or dropdown),
currency,
avatar,
phone,
mobile(mobile vlaidation),
jsonfield (json validation),
photo (allow selecting photo),
gallery (allow selecting anythin from storage)

creditcard (card 4444 4444 4444),
debitcard(card),
moneycard(card),

barcode,
qrcode,
social@facebook,
static,
header,

// html input types


